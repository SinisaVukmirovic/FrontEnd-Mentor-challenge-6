---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title='Services'>

    <div class="services">

        <div class="text">
            <h1>Service</h1>
            <h2>Plan out <span>project</span>
                <br>
                 carefully!</h2>
            <p>So you don't need service!</p>
            <p>Don't make a mistake like me, not being able to reuse header component, because of not planing out right...</p>
        </div>

        <div class="img">
            <picture>
                <source srcset="/images/desktop/image-gallery-sugarcubes.jpg" media="(min-width: 1440px)">
                <img src="/images/mobile/image-gallery-sugar-cubes.jpg" alt="Graphics image">
            </picture>
        </div>
    </div>

</MainLayout>

<style>
    img {
        width: 100%;
    }

    .services {
            font-family: var(--fraunces);
            /* line-height: 1.5; */
            display: grid;
            grid-template-columns: 1fr;
            padding: 1rem;
            background-color: var(--White);            
            
            /* !!! made a mistake in planing out, so i can't reuse header, have to resort to this */
            position: absolute;
            top: 6rem;
            height: 38rem;
        }

        .text {
            order: 2;
            padding: 1rem;
            color: var(--Very-dark-grayish-blue);
        }
        
        h1 {
            font-size: 1rem;
            margin-right: 1rem;
        }
    
        h2 {
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }
            h2 span {
                color: var(--Soft-red);
                font-size: 3rem;
                font-weight: 900;
            }
            
            p {
                line-height: 1.25;
            }

    @media (min-width: 1440px) {
        .services {
            font-family: var(--fraunces);
            /* line-height: 1.5; */
            display: grid;
            grid-template-columns: 1fr 2fr;
            padding: 6rem;
            /* background-color: var(--White); */
            /* background-image: linear-gradient(to top, var(--bot-blue), var(--top-blue)); */
            
            
            /* !!! made a mistake in planing out, so i can't reuse header, have to resort to this */
            position: absolute;
            top: 6rem;
            height: 42rem;
        }
        .text {
            padding: 5rem;
            color: var(--Very-dark-grayish-blue);
        }
        
        h1 {
            font-size: 1.5rem;
            margin-left: 3rem;
            margin-bottom: 1rem;
        }
    
        h2 {
            margin-bottom: 2rem;
            font-size: 2.5rem;
        }
            h2 span {
                /* color: var(--blue); */
                font-size: 5rem;
                /* font-weight: 900; */
            }
            
            p {
                line-height: 1.5;
            }
    }

</style>